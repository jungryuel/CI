spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: ${user}
    password: ${password}
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  application:
    name: devops